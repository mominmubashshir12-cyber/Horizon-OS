# Set this to your Railway backend URL once the backend is deployed.
# Note: the backend mounts its API under /api, so include the /api suffix.
# Example: https://horizon-backend.up.railway.app/api
NEXT_PUBLIC_API_URL=https://your-railway-backend-url.up.railway.app/api
